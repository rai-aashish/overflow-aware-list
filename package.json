{
  "name": "overflow-aware-list-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck",
    "clean": "bun run --filter '*' clean",
    "prepare": "husky",
    "publish-packages": "node scripts/publish.mjs"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "typescript": "^5.8.0"
  }
}
